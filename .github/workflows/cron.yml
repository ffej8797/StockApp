name: Stock Cron

on:
  schedule:
    - cron: "*/10 * * * *"

  workflow_dispatch:

jobs:
  cron:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger cron API
        run: |
          curl -X GET https://stock-app-two-kappa.vercel.app/clock